# Use root/example as user/password credentials
version: '3.1'

services:

  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password --lower_case_table_names=1
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: Ytsy3113678AB
    ports:
      - 3306:3306

  #adminer:
   # image: adminer
   # restart: always
   # ports:
   #   - 8080:8080